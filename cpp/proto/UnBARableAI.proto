syntax = "proto3";

import "google/protobuf/empty.proto";

// Service for communication between engine and rl environment
service UnBARableAIService {
    rpc handleEventUpdate (google.protobuf.Empty) returns (google.protobuf.Empty);
}