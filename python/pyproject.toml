[project]
name = "UnBARableAIEnvironment"
version = "0.1.0"
description = "Environment for the UnBARableAI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.83.0",
    "grpcio-tools>=1.81.1",
    "pybind11>=3.0.4",
    "numpy",
    "pytest>=8.0",
    "torch>=2.12.0",
    "wandb>=0.27.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.scikit-build]
wheel.packages = ["src"]

[build-system]
requires = ["scikit-build-core", "pybind11", "grpcio-tools"]
build-backend = "scikit_build_core.build"
